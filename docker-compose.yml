networks:
  default:
    external: true
    name: roboshopnw
services:
  mongodb:
    image: mongdb:v1.0
    container_name: mongodb
    
  catalogue:
    image: catalogue:v1.0
    container_name: catalogue
    depends_on: 
      - mongodb
  web:
   image: web:v1.0
   container_name: web
   ports:
      - "80:80"
   depends_on: 
      - catalogue
